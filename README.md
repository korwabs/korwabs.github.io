# WABS Landing Page

Public landing page for WABS, built with Next.js static export and deployed through GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run lint
npm run build
```

## Deploy

```bash
npm run deploy
```

The deploy command exports the site to `out/` and publishes it to the `gh-pages` branch.
